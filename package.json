{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  	"start": "babel-node --presets es2015 src/index.js",
    "start-dev": "nodemon --exec babel-node --presets es2015 src/index.js",
    "clean-server": "rm -rf build && mkdir build",
    "build-server": "babel src --out-dir build"
  },
  "author": "Tuomo Kankaanpää",
  "license": "ISC",
  "dependencies": {
  	"babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "express": "^4.14.0",
    "socket.io": "^1.7.2"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
